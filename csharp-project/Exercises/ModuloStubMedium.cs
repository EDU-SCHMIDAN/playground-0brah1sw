// { autofold
using System;
using System.Linq;

namespace Answer
{
	public class ModuloStubMedium
	{
// }
public static bool IsLeapYear(int year)
{
    bool response = false;

    /*
     * la variable response indique si year est une année bissextile (comportant 366 jours)
     * ceci est vrai si elle respecte l’un des deux critères suivants :
     * C1: l'année est divisible par 4 sans être divisible par 100 (cas des années qui ne sont pas des multiples de 100) ;
     * C2: l'année est divisible par 400 (cas des années multiples de 100)
     */


    return response;
}
//{ autofold
	}
}
//}
